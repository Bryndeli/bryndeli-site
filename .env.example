# Copy to .env (gitignored) for local dev, or set in Vercel project settings.
# Names only — never commit real values.

# Airtable — waiting list storage
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=

# Brevo — waiting list auto-reply email
BREVO_API_KEY=
MAIL_FROM_ADDRESS=
